NEXT_PUBLIC_API_URL=https://api.cliente.tudominio.com/api
NEXT_PUBLIC_WS_URL=https://api.cliente.tudominio.com
# Key del NAVEGADOR para Google Maps (Maps JavaScript API + Geocoding API).
# Restringida por HTTP referrer (dominio del panel) en producción. Distinta de la
# key de servidor (GOOGLE_MAPS_SERVER_KEY) que usa el routing del API.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
